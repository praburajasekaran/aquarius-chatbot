---
phase: 04-validation
plan: 04
type: execute
wave: 2
depends_on: ["04-02", "04-03"]
files_modified:
  - .planning/phases/04-validation/04-UAT-EVIDENCE.md
  - .planning/phases/04-validation/screenshots/sc2-smokeball-invoice-line.png
  - .planning/phases/04-validation/screenshots/sc2-zapier-run.png
autonomous: false
requirements: [TEST-02]
user_setup:
  - service: smokeball
    why: "Firm must log in to Smokeball, find the test matter, and screenshot the invoice line items — Claude has no programmatic access"
    dashboard_config:
      - task: "Log in to Smokeball, locate matter/invoice created by Zapier for the UAT test transaction"
        location: "Smokeball web app (firm's workspace)"
      - task: "Screenshot the invoice line items and email/Slack the screenshot to the operator"
        location: "Firm's device → email/Slack"
  - service: zapier
    why: "Operator verifies Zapier run in dashboard — no programmatic access"
    dashboard_config:
      - task: "Verify the Zap run that created the Smokeball matter succeeded (Zap #1 PROD)"
        location: "Zapier Dashboard → Task History"
must_haves:
  truths:
    - "Zapier Zap #1 (transcript → Smokeball Create Matter) fired successfully for the test transaction — screenshot attached"
    - "Smokeball invoice line items are byte-identical to `PRICING[urgency].lineItem` from `src/lib/pricing.ts` — NO truncation, NO reformatting, NO HTML entity drift"
    - "If any truncation/drift is observed, it is diagnosed (BPoint MerchantReference vs Smokeball display vs Zapier mapping) and recorded"
  artifacts:
    - path: ".planning/phases/04-validation/screenshots/sc2-smokeball-invoice-line.png"
      provides: "Firm-captured screenshot of the Smokeball invoice line items"
    - path: ".planning/phases/04-validation/screenshots/sc2-zapier-run.png"
      provides: "Zapier dashboard screenshot of the transcript → Smokeball Create Matter run"
    - path: ".planning/phases/04-validation/04-UAT-EVIDENCE.md"
      provides: "TEST-02 row filled with byte-compare verdict (MATCH / TRUNCATED / DRIFTED) + transcribed screenshot text + MerchantReference log snippet"
  key_links:
    - from: ".planning/phases/04-validation/04-UAT-EVIDENCE.md"
      to: "src/lib/pricing.ts"
      via: "byte-compare assertion against PRICING.urgent.lineItem + PRICING[\"non-urgent\"].lineItem"
      pattern: "Initial Deposit for Urgent Court Matter"
---

<objective>
Close TEST-02: the single most legally load-bearing assertion in Phase 4 — byte-compare the Smokeball invoice line item string against `src/lib/pricing.ts` PRICING constants to prove the Zapier contract survived the Stripe→BPoint migration. Binary pass/fail, no wiggle room.

Purpose: Prove that `MerchantReference` (set in `src/lib/bpoke.ts` from `PRICING[urgency].lineItem`) flows through BPoint → callback → retrieveTransaction → Zapier transcript email → Zapier Zap → Smokeball invoice WITHOUT truncation, encoding, or drift. Any mismatch (even a trailing ellipsis) fails TEST-02 and diagnosis must identify which stage truncated.
Output: Two screenshot files + fully-populated TEST-02 row in `04-UAT-EVIDENCE.md` with byte-compare verdict.
</objective>

<execution_context>
@/Users/praburajasekaran/.claude/get-shit-done/workflows/execute-plan.md
@/Users/praburajasekaran/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-validation/04-CONTEXT.md
@.planning/phases/04-validation/04-RESEARCH.md
@.planning/phases/04-validation/04-VALIDATION.md
@.planning/phases/04-validation/04-RUNBOOK.md
@.planning/phases/04-validation/04-UAT-EVIDENCE.md
@.planning/phases/04-validation/04-02-SUMMARY.md
@.planning/phases/04-validation/04-03-SUMMARY.md
@src/lib/pricing.ts
@src/lib/bpoint.ts
@src/lib/resend.ts

<interfaces>
<!-- Byte-compare ground truth. These strings are the ONLY thing that matters for TEST-02. -->

From src/lib/pricing.ts — exact bytes (no trailing spaces, no smart quotes, no Unicode dashes):
```typescript
PRICING.urgent.lineItem        === "Initial Deposit for Urgent Court Matter"  // 38 chars
PRICING["non-urgent"].lineItem === "Legal Strategy Session"                  // 23 chars
```

From src/lib/bpoint.ts (createAuthKey):
- `MerchantReference: PRICING[args.urgency].lineItem` — passed to BPoint verbatim at AuthKey creation.

From src/lib/resend.ts (Zapier transcript email — `sendTranscriptEmail`):
- HTML row labels "BPoint Transaction" (transcript) / "BPoint transaction" (booking).
- The `lineItem` string appears in the transcript HTML and is parsed by Zapier into a Smokeball line-item field.

Expected flow (what TEST-02 proves):
```
src/lib/pricing.ts PRICING.urgent.lineItem
  → src/lib/bpoint.ts createAuthKey MerchantReference
  → BPoint stores MerchantReference
  → webhook/confirm retrieveTransaction TxnResp.MerchantReference  (same bytes)
  → handleConfirmedPayment passes to sendTranscriptEmail
  → transcript email HTML contains the lineItem text
  → Zapier parses + maps to Smokeball "line items" field
  → Smokeball displays the line item on the invoice/matter
```

Known drift risks (Pitfall 5 from RESEARCH.md):
- BPoint may truncate `MerchantReference` to 24 chars (legacy gateway limit) → "Initial Deposit for Urge" instead of full string
- Zapier may HTML-decode or URL-encode fields asymmetrically
- Smokeball may display a truncated preview in invoice line rows
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Firm captures Smokeball invoice screenshot + operator verifies Zapier run</name>
  <files>.planning/phases/04-validation/screenshots/sc2-smokeball-invoice-line.png, .planning/phases/04-validation/screenshots/sc2-zapier-run.png, .planning/phases/04-validation/04-UAT-EVIDENCE.md</files>
  <action>Operator emails/Slacks the firm rep requesting a Smokeball screenshot of the invoice line items for the UAT test matter (identify by bpointTxnNumber or clientName "UAT TEST — delete after sign-off"). Firm replies with screenshot; operator saves to screenshots/sc2-smokeball-invoice-line.png. Operator (or firm, if access-restricted) captures Zapier Task-History success screenshot for Zap #1 transcript → Smokeball Create Matter run and saves to screenshots/sc2-zapier-run.png. Operator greps Vercel logs of the Plan 02 happy-path run for `MerchantReference` line, pastes that log line into the "MerchantReference outbound log" slot in 04-UAT-EVIDENCE.md TEST-02 section. If outbound log shows truncation (not the full PRICING lineItem string), flag and escalate — this is a Phase 1/3 regression, not a TEST-02 result.</action>
  <what-built>
    This step requires a firm rep to log in to Smokeball and screenshot the UAT test matter's invoice line items. Smokeball has no programmatic API access within scope (deferred to v2 per RESEARCH.md). The operator (Claude) then verifies Zapier's Task History shows the run succeeded.

    **Claude cannot automate any of this.** Smokeball is firm-only access; Zapier dashboard is behind the firm's SSO.
  </what-built>
  <how-to-verify>
    **Part A — Firm (Smokeball screenshot):**
    1. Operator emails/Slacks the firm: "Please open Smokeball, find the matter created by Zapier for test transaction `<bpointTxnNumber>` (created during Plan 02 happy-path run on <date>). Screenshot the invoice line items (full text, no cropping). Email/Slack the screenshot back. If the matter is hard to find: search by `clientName: \"UAT TEST — delete after sign-off\"` if you used the distinctive-label convention, or filter by date of the test run."
    2. Firm replies with screenshot.
    3. Operator saves screenshot → `.planning/phases/04-validation/screenshots/sc2-smokeball-invoice-line.png`.

    **Part B — Operator (Zapier verification):**
    4. If operator has Zapier dashboard access (firm-shared or firm-forwarded), navigate to Zapier → Task History → filter for Zap #1 (transcript → Smokeball Create Matter) for the test run's timestamp. Screenshot the successful run. Save to `.planning/phases/04-validation/screenshots/sc2-zapier-run.png`.
    5. If operator does NOT have access: ask firm to include the Zapier screenshot in the same email as Part A.

    **Part C — Pre-compare sanity check:**
    6. From Vercel logs of the happy-path run (Plan 02), `grep -E "MerchantReference" <vercel-log>` and confirm the outbound BPoint POST contained the full `PRICING.urgent.lineItem` string (e.g. "MerchantReference: \"Initial Deposit for Urgent Court Matter\""). Paste the log line into 04-UAT-EVIDENCE.md TEST-02 "MerchantReference outbound log:" slot.
    7. If the outbound log shows truncation: this is a BPoint client bug — NOT a TEST-02 discovery, but a Phase 1/3 regression. Flag and escalate.

    **Verify:**
    - `test -f .planning/phases/04-validation/screenshots/sc2-smokeball-invoice-line.png`
    - `test -f .planning/phases/04-validation/screenshots/sc2-zapier-run.png`
    - `grep -F "MerchantReference" .planning/phases/04-validation/04-UAT-EVIDENCE.md` returns >= 1 match
  </how-to-verify>
  <verify>
    <automated>test -f .planning/phases/04-validation/screenshots/sc2-smokeball-invoice-line.png && test -f .planning/phases/04-validation/screenshots/sc2-zapier-run.png && grep -F "MerchantReference" .planning/phases/04-validation/04-UAT-EVIDENCE.md</automated>
  </verify>
  <done>Both screenshots (sc2-smokeball-invoice-line.png + sc2-zapier-run.png) exist in screenshots/ dir; MerchantReference outbound log line pasted into TEST-02 section of 04-UAT-EVIDENCE.md; any truncation flagged for escalation rather than recorded as a TEST-02 finding.</done>
  <resume-signal>Type "screenshots captured" with both files in place, or "firm needs reminder" / "Zapier access issue" / "MerchantReference log shows truncation — escalating"</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Byte-compare transcribed Smokeball text against PRICING and populate TEST-02 evidence</name>
  <files>.planning/phases/04-validation/04-UAT-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/04-validation/screenshots/sc2-smokeball-invoice-line.png (VIEW the image — transcribe the exact text from the line-item field, character-by-character)
    - src/lib/pricing.ts (ground-truth strings — every byte matters)
    - .planning/phases/04-validation/04-UAT-EVIDENCE.md (current state — only TEST-02 section should be modified)
    - .planning/phases/04-validation/04-RESEARCH.md §"Pitfall 5: Mismatched lineItem string" (drift diagnosis matrix)
  </read_first>
  <action>
    **Step 1 — Transcribe:**
    Open `.planning/phases/04-validation/screenshots/sc2-smokeball-invoice-line.png` using the Read tool (which supports images). Transcribe the EXACT text of the invoice line item as it appears in Smokeball. Be hyper-literal:
    - No paraphrasing
    - Preserve every space, punctuation mark, ellipsis, quotation mark, and trailing/leading whitespace
    - If text is truncated in the screenshot (ends with "…" or is visibly cut off), transcribe the truncation exactly — do NOT expand or assume

    **Step 2 — Byte-compare:**
    Compare the transcribed text against the two canonical strings:
    - `"Initial Deposit for Urgent Court Matter"` (PRICING.urgent.lineItem — 38 chars)
    - `"Legal Strategy Session"` (PRICING["non-urgent"].lineItem — 23 chars)

    Determine verdict:
    - **MATCH:** Transcribed text is byte-identical to one of the two canonical strings. TEST-02 passes.
    - **TRUNCATED:** Transcribed text is a proper prefix of one of the canonical strings (e.g. "Initial Deposit for Urgent Court Ma…" or "Initial Deposit for Urge"). TEST-02 fails; diagnose which stage truncated per Pitfall 5.
    - **DRIFTED:** Transcribed text contains reordered/rephrased/re-encoded content that is not a proper prefix (e.g. "Urgent Court Matter — Initial Deposit" or "Initial Deposit for Urgent Court Matter&nbsp;"). TEST-02 fails; diagnose the encoding/transformation stage.

    **Step 3 — Populate 04-UAT-EVIDENCE.md TEST-02 section:**

    Edit `.planning/phases/04-validation/04-UAT-EVIDENCE.md`. Find the `## TEST-02` section. Replace the placeholder content with:

    ```markdown
    ## TEST-02 — Smokeball invoice line items reconcile byte-for-byte

    | Status | Evidence |
    |--------|----------|
    | {✅ MATCH | ❌ TRUNCATED | ❌ DRIFTED} | (filled below) |

    ### Byte-compare assertion

    **Expected (from `src/lib/pricing.ts`):**
    - `PRICING.urgent.lineItem` = `Initial Deposit for Urgent Court Matter` (38 chars)
    - `PRICING["non-urgent"].lineItem` = `Legal Strategy Session` (23 chars)

    **Tier tested in this UAT run:** {urgent | non-urgent}

    **Observed (transcribed from firm's Smokeball screenshot `sc2-smokeball-invoice-line.png`):**
    ```
    {paste the EXACT transcribed text here, inside the code block — preserve every byte}
    ```

    **Character count of observed:** {count} chars
    **Character count of expected:** {38 or 23} chars

    **Verdict:** {✅ MATCH | ❌ TRUNCATED | ❌ DRIFTED}

    {IF MATCH:}
    - Byte-for-byte identical to `PRICING.{urgent|non-urgent}.lineItem`.
    - Zapier + Smokeball preserved the string end-to-end.
    - TEST-02 PASSES.

    {IF TRUNCATED:}
    - Transcribed text is a prefix of the canonical string.
    - Diagnosis checklist (per .planning/phases/04-validation/04-RESEARCH.md Pitfall 5):
      - [ ] BPoint MerchantReference outbound log (this file, below) shows FULL string → truncation is at BPoint or Zapier or Smokeball (eliminate our code).
      - [ ] `grep -F "{full lineItem}" <vercel-log>` on the confirm-route fan-out log → if FULL: truncation is downstream (Zapier/Smokeball).
      - [ ] Check Zapier task details: inspect the data passed from Webhooks-step to Smokeball-step → if truncated there: Zapier's parser issue.
      - [ ] If FULL in Zapier outbound but TRUNCATED in Smokeball display: Smokeball UI preview issue, not actual data corruption. Ask firm to click the line item to view the full detail.
    - TEST-02 FAILS until diagnosed + resolved.

    {IF DRIFTED:}
    - Transcribed text is NOT a prefix — content was reordered, re-encoded, or replaced.
    - Likely cause: HTML entity encoding (e.g. `&nbsp;`, `&amp;`), URL-encoding, or Zapier template rewrite.
    - Diagnosis: inspect Zapier task's raw input vs output; check `src/lib/resend.ts` transcript HTML for any entity encoding on the lineItem field.
    - TEST-02 FAILS until diagnosed.

    ### Artefacts
    - **Smokeball invoice line screenshot:** `screenshots/sc2-smokeball-invoice-line.png` (captured by firm)
    - **Zapier run screenshot:** `screenshots/sc2-zapier-run.png` (captured by operator or firm)
    - **MerchantReference outbound log (from Vercel `[bpoint]` tag, Plan 02 run):**
      ```
      {paste the grep "MerchantReference" line from Vercel logs — must contain the full lineItem text if the outbound path is clean}
      ```
    ```

    Do NOT edit any other section of `04-UAT-EVIDENCE.md`. Only the `## TEST-02` section.

    **Step 4 — Update bundle header status:**
    At the top of the file, update the `score:` frontmatter line to reflect the updated count (e.g. `score: 1/3 SCs verified` if this is the first SC marked, or `2/3` if Plan 02 already marked TEST-01).
  </action>
  <verify>
    <automated>grep -c "TEST-02 — Smokeball" .planning/phases/04-validation/04-UAT-EVIDENCE.md | awk '{exit ($1 < 1)}' && grep -E "Verdict:.*(MATCH|TRUNCATED|DRIFTED)" .planning/phases/04-validation/04-UAT-EVIDENCE.md && grep -F "Initial Deposit for Urgent Court Matter" .planning/phases/04-validation/04-UAT-EVIDENCE.md && grep -F "Legal Strategy Session" .planning/phases/04-validation/04-UAT-EVIDENCE.md && grep -E "Character count of observed:" .planning/phases/04-validation/04-UAT-EVIDENCE.md && grep -F "sc2-smokeball-invoice-line.png" .planning/phases/04-validation/04-UAT-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "## TEST-02" .planning/phases/04-validation/04-UAT-EVIDENCE.md` returns 1
    - `grep -E "Verdict:.*(MATCH|TRUNCATED|DRIFTED)" .planning/phases/04-validation/04-UAT-EVIDENCE.md` prints one match (verdict committed)
    - `grep -F "Initial Deposit for Urgent Court Matter" .planning/phases/04-validation/04-UAT-EVIDENCE.md` returns match (expected canonical string present)
    - `grep -F "Legal Strategy Session" .planning/phases/04-validation/04-UAT-EVIDENCE.md` returns match (expected canonical string present)
    - `grep -E "Character count of observed: [0-9]+" .planning/phases/04-validation/04-UAT-EVIDENCE.md` returns match (numeric count present)
    - `grep -F "sc2-smokeball-invoice-line.png" .planning/phases/04-validation/04-UAT-EVIDENCE.md` returns match
    - If verdict is MATCH: `grep -E "score: [12]/3 SCs" .planning/phases/04-validation/04-UAT-EVIDENCE.md` returns match (bundle score updated)
    - Observed-text block is present (under `**Observed.*transcribed` line) and contains non-placeholder text (no `{paste` literal strings remaining)
  </acceptance_criteria>
  <done>TEST-02 section of 04-UAT-EVIDENCE.md is filled with exact byte-compare verdict, transcribed observed text, character counts, MerchantReference outbound log, and artifact references. Bundle header score updated.</done>
</task>

</tasks>

<verification>
- `test -f .planning/phases/04-validation/screenshots/sc2-smokeball-invoice-line.png`
- `test -f .planning/phases/04-validation/screenshots/sc2-zapier-run.png`
- `grep -E "Verdict:.*(MATCH|TRUNCATED|DRIFTED)" .planning/phases/04-validation/04-UAT-EVIDENCE.md` committed
- If MATCH: TEST-02 row marked ✅; if TRUNCATED/DRIFTED: diagnosis checklist populated + failure noted
- `grep -F "Initial Deposit for Urgent Court Matter" .planning/phases/04-validation/04-UAT-EVIDENCE.md` returns match (ground truth documented)
</verification>

<success_criteria>
- Firm's Smokeball screenshot captured + saved
- Zapier run screenshot captured + saved
- Byte-compare verdict (MATCH/TRUNCATED/DRIFTED) committed to evidence bundle
- If MATCH: TEST-02 PASSES, bundle score moves toward 3/3
- If FAIL: diagnosis checklist committed identifying which stage truncated/drifted; Plan 05 cutover gate will block until resolved
- TEST-02 requirement addressed (either satisfied or blocked with clear next action)
</success_criteria>

<output>
After completion, create `.planning/phases/04-validation/04-04-SUMMARY.md` capturing: screenshots captured, transcribed Smokeball text verbatim, byte-compare verdict, character counts, any diagnosis if verdict != MATCH, links to follow-up (e.g. Zapier template fix, BPoint support ticket, Smokeball UI investigation).
</output>
