---
phase: 04-validation
plan: 05
type: execute
wave: 2
depends_on: ["04-02", "04-03", "04-04"]
files_modified:
  - .planning/phases/04-validation/04-UAT-EVIDENCE.md
  - .planning/phases/04-validation/04-RUNBOOK.md
autonomous: false
requirements: [TEST-01, TEST-02, TEST-03]
user_setup:
  - service: firm-sign-off
    why: "Cutover gate requires firm to ack all three SCs in writing (email or Slack)"
    dashboard_config:
      - task: "Firm rep replies in email or Slack acknowledging TEST-01/02/03 all green after reviewing 04-UAT-EVIDENCE.md"
        location: "Firm's email or Slack"
must_haves:
  truths:
    - "TEST-01, TEST-02, TEST-03 all marked ✅ in `04-UAT-EVIDENCE.md` cutover-gate checklist"
    - "Firm rep has acknowledged sign-off in writing — verbatim quote captured in evidence bundle footer with name, date, medium"
    - "`04-RUNBOOK.md` §9 rollback procedure documents the exact Vercel redeploy steps + manual fan-out replay option"
    - "Evidence bundle frontmatter `sign_off:` block is fully populated (no `pending` values)"
    - "Production cutover is now unblocked — the firm or ops lead can proceed with `git merge` + Vercel production promotion"
  artifacts:
    - path: ".planning/phases/04-validation/04-UAT-EVIDENCE.md"
      provides: "Fully-populated evidence bundle with cutover-gate checklist all ticked + firm sign-off quote"
      contains: "Firm sign-off"
    - path: ".planning/phases/04-validation/04-RUNBOOK.md"
      provides: "Runbook §9 rollback procedure expanded with concrete Vercel-redeploy commands + manual fan-out replay runbook"
      contains: "Rollback procedure"
  key_links:
    - from: ".planning/phases/04-validation/04-UAT-EVIDENCE.md"
      to: "sign_off (firm)"
      via: "verbatim quoted reply with name + date + medium in footer"
      pattern: "firm_rep:"
    - from: ".planning/phases/04-validation/04-RUNBOOK.md"
      to: ".planning/phases/04-validation/04-UAT-EVIDENCE.md"
      via: "§9 rollback ties back to the evidence bundle as the cutover gate"
      pattern: "04-UAT-EVIDENCE.md"
---

<objective>
Close the Phase 4 cutover gate: consolidate TEST-01 + TEST-02 + TEST-03 evidence into a sign-off-ready bundle, capture the firm's written acknowledgement, and expand the runbook's §9 rollback procedure with concrete Vercel-redeploy + manual fan-out replay steps.

Purpose: Production cutover is gated on this plan. Nothing ships until all three SCs show ✅ in the evidence bundle AND the firm has replied in writing. This plan produces the definitive "OK to deploy" artifact.
Output: `04-UAT-EVIDENCE.md` with cutover checklist 4/4 ticked + firm sign-off footer populated; `04-RUNBOOK.md` §9 fleshed out with concrete commands.
</objective>

<execution_context>
@/Users/praburajasekaran/.claude/get-shit-done/workflows/execute-plan.md
@/Users/praburajasekaran/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-validation/04-CONTEXT.md
@.planning/phases/04-validation/04-RESEARCH.md
@.planning/phases/04-validation/04-VALIDATION.md
@.planning/phases/04-validation/04-RUNBOOK.md
@.planning/phases/04-validation/04-UAT-EVIDENCE.md
@.planning/phases/04-validation/04-02-SUMMARY.md
@.planning/phases/04-validation/04-03-SUMMARY.md
@.planning/phases/04-validation/04-04-SUMMARY.md
@src/lib/payments/handleConfirmedPayment.ts
@CLAUDE.md

<interfaces>
<!-- Sign-off contract + rollback context -->

Cutover gate contract (from 04-CONTEXT.md §"Evidence, sign-off, cutover"):
- All three SCs must show ✅ in 04-UAT-EVIDENCE.md
- Firm acknowledges in writing (email or Slack reply — no legal signature required)
- Any red (❌) → no cutover

Rollback context (from 04-CONTEXT.md):
- Stripe is fully removed in Phase 3 — "rollback to Stripe" is not possible
- Rollback = Vercel redeploy to last known-good BPoint commit on main
- ≤5 min procedure
- Manual fan-out replay: use `bpointTxnNumber` + logs to re-run handleConfirmedPayment if Resend or Zapier flap post-cutover

Evidence bundle frontmatter shape (already stubbed in Plan 01, filled incrementally by Plans 02-04):
```yaml
sign_off:
  firm_rep: {name}
  date: {ISO date or YYYY-MM-DD}
  medium: {email | slack | other}
  quote: |
    {verbatim multi-line quote from firm's reply}
```

Structured-log shape for manual replay (from handleConfirmedPayment):
- `[payments]` tag with `bpointTxnNumber`, `sessionId`, `phase` ("session-update" | "upload-token" | "receipt-email" | "transcript-email"), `err.message`, `err.stack`
- Enough for support to reproduce or manually re-run a specific step
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand RUNBOOK §9 rollback procedure with concrete commands</name>
  <files>.planning/phases/04-validation/04-RUNBOOK.md</files>
  <read_first>
    - .planning/phases/04-validation/04-RUNBOOK.md (current state — only §9 should be modified; other sections stay)
    - src/lib/payments/handleConfirmedPayment.ts (to cite the exact phase tags in the manual replay section)
    - .planning/phases/04-validation/04-CONTEXT.md §"Evidence, sign-off, cutover" (locks rollback decisions)
  </read_first>
  <action>
    Locate the existing `## §9. Rollback procedure (cutover safety)` section in `04-RUNBOOK.md`. REPLACE the section's body with the expanded version below. Do NOT modify §1-§8 or §10.

    New §9 content (keep the `## §9.` heading line; replace the body beneath it until the next `## §` heading):

    ```markdown
    ## §9. Rollback procedure (cutover safety)

    **Trigger:** Post-cutover incident (checkout broken, webhook fan-out silent, receipt emails failing, Smokeball ingest stopped). Decision owner: firm ops lead. Target RTO: ≤5 minutes.

    ### §9.1 Vercel redeploy to last known-good

    - [ ] Identify last known-good commit on `main` (BEFORE the Phase 4 cutover merge). Check `git log --oneline main` — pick the commit that was in production immediately before Phase 4 went live.
    - [ ] Option A (dashboard, ≤3 min): Vercel Dashboard → Project → Deployments → find the Production deployment for that commit SHA → click ⋯ → "Promote to Production" (or "Redeploy"). Confirm. Vercel rolls the alias in <60s.
    - [ ] Option B (CLI, ≤2 min): `vercel rollback <deployment-url>` where `<deployment-url>` is the last-known-good deployment URL from `vercel list`. Example: `vercel rollback https://aquarius-chatbot-abc123.vercel.app --scope=<team>`.
    - [ ] Verify: `curl -s https://aquariuslawyers.com.au/api/checkout -H 'Content-Type: application/json' -d '{"sessionId":"rollback-probe","urgency":"urgent"}' | jq .` returns a valid BPoint authKey shape (rollback is effective when checkout works end-to-end, not just when the deploy changes).
    - [ ] Sanity: visit `https://aquariuslawyers.com.au` in an incognito window, trigger chat to payment step, confirm iframe renders.

    ### §9.2 Reminder: Stripe is gone

    Rollback means reverting to an **earlier BPoint commit** — NOT reverting to Stripe.
    - `src/lib/stripe.ts` was deleted in Phase 3-04
    - `src/app/api/webhooks/stripe/route.ts` was deleted in Phase 3-04
    - `stripe`, `@stripe/stripe-js`, `@stripe/react-stripe-js` were uninstalled
    - Any commit that still has Stripe code is pre-Phase-3 and will not build cleanly against current env vars (STRIPE_* variables are removed from Vercel prod env)

    If a true "fully back to Stripe" rollback becomes necessary (unexpected), it requires a separate restoration PR + Stripe package reinstall + Vercel env var restoration. Not part of this runbook.

    ### §9.3 Manual fan-out replay (for partial failures)

    Use when: Cutover succeeded but a downstream step failed for a specific transaction (e.g. Resend 5xx blocked a receipt email, Zapier filter rejected the transcript, Upstash Redis was briefly unavailable when upload-token was being created).

    The shared fan-out helper `src/lib/payments/handleConfirmedPayment.ts` logs structured failures with tag `[payments]` and a `phase` field identifying which step failed. Available phases:
    - `"session-update"` — Redis session mark-as-paid
    - `"upload-token"` — upload-token creation
    - `"receipt-email"` — Resend receipt to client
    - `"transcript-email"` — Zapier transcript (Zap #1 → Smokeball Create Matter)

    - [ ] **Locate the failure:** `vercel logs <production-url> | grep "\[payments\]" | grep -E "phase.*error" | tail -20`. Copy the `bpointTxnNumber` + `sessionId` + failed `phase` from the log line.
    - [ ] **Decide replay step(s):** fan-out steps are independent — replay only the failed phase(s), not all four.
    - [ ] **Replay via one-off script** (preferred): create a scratch file `scripts/replay-fanout.ts` that imports `handleConfirmedPayment` + the phase-specific helper, and invokes ONLY the failed step against the stored intake for that `bpointTxnNumber`. Run with `npx tsx scripts/replay-fanout.ts <bpointTxnNumber> <phase>`. Remove the script after use (do not commit).
    - [ ] **Replay via direct helper invocation** (fallback, for `receipt-email` only): open a Node REPL on the production env (`vercel dev --prod-env`) and call `sendReceiptEmail({...intake})` directly after fetching the intake from Redis.
    - [ ] **Do NOT delete the `bpoint-txn:{TxnNumber}` dedup key** — the dedup namespace protects against accidental double-fan-out from webhook retries. Replay the specific phase directly; don't reset the lock.
    - [ ] **Confirm remediation:** Resend dashboard shows the re-sent email; Zapier Task History shows the re-fired transcript task; Redis `bpoint-txn:{TxnNumber}` key still present.

    ### §9.4 Escalation

    If rollback fails OR the incident is >15 minutes old without resolution:
    - Contact BPoint support (1300 766 031) — could be BPoint-side outage
    - Contact Vercel support (dashboard → Help)
    - Page firm ops lead — they own the decision to hold/resume the chat's payment step (can disable the payment-card feature flag if one exists, or render a "payments temporarily unavailable" message)
    ```

    Preserve all other runbook sections (§1-§8 and §10) byte-identically.
  </action>
  <verify>
    <automated>grep -c "^## §9" .planning/phases/04-validation/04-RUNBOOK.md | awk '{exit ($1 < 1)}' && grep -c "^### §9\." .planning/phases/04-validation/04-RUNBOOK.md | awk '{exit ($1 < 4)}' && grep -F "vercel rollback" .planning/phases/04-validation/04-RUNBOOK.md && grep -F "session-update\|upload-token\|receipt-email\|transcript-email" .planning/phases/04-validation/04-RUNBOOK.md && grep -F "Stripe is gone" .planning/phases/04-validation/04-RUNBOOK.md && grep -c "^## §10" .planning/phases/04-validation/04-RUNBOOK.md | awk '{exit ($1 < 1)}' && grep -c "^## §1\." .planning/phases/04-validation/04-RUNBOOK.md | awk '{exit ($1 < 1)}'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## §9" .planning/phases/04-validation/04-RUNBOOK.md` returns 1 (§9 heading preserved)
    - `grep -c "^### §9\." .planning/phases/04-validation/04-RUNBOOK.md` returns at least 4 (§9.1, §9.2, §9.3, §9.4 subsections)
    - `grep -F "vercel rollback" .planning/phases/04-validation/04-RUNBOOK.md` returns match (CLI command documented)
    - `grep -F "Promote to Production" .planning/phases/04-validation/04-RUNBOOK.md` returns match (dashboard option documented)
    - `grep -F "session-update" .planning/phases/04-validation/04-RUNBOOK.md` returns match (phase tag cited)
    - `grep -F "upload-token" .planning/phases/04-validation/04-RUNBOOK.md` returns match
    - `grep -F "receipt-email" .planning/phases/04-validation/04-RUNBOOK.md` returns match
    - `grep -F "transcript-email" .planning/phases/04-validation/04-RUNBOOK.md` returns match
    - `grep -F "Stripe is gone" .planning/phases/04-validation/04-RUNBOOK.md` returns match
    - `grep -c "^## §10" .planning/phases/04-validation/04-RUNBOOK.md` returns 1 (§10 Warnings section preserved)
    - `grep -c "^## §1\." .planning/phases/04-validation/04-RUNBOOK.md` returns 1 (§1 section preserved)
    - `wc -l .planning/phases/04-validation/04-RUNBOOK.md` returns a line count greater than the pre-edit line count (content added, nothing truncated)
  </acceptance_criteria>
  <done>Runbook §9 rollback procedure has 4 subsections (Vercel redeploy, Stripe-gone reminder, manual fan-out replay, escalation) with concrete CLI + dashboard commands and phase-tag references; §1-§8 and §10 preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Consolidate final cutover-gate checklist in 04-UAT-EVIDENCE.md</name>
  <files>.planning/phases/04-validation/04-UAT-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/04-validation/04-UAT-EVIDENCE.md (current state — Plans 02-04 have filled most sections; this task is a consolidation + gap-fill pass)
    - .planning/phases/04-validation/04-02-SUMMARY.md, 04-03-SUMMARY.md, 04-04-SUMMARY.md (what each plan recorded — cross-check)
    - .planning/phases/04-validation/04-CONTEXT.md §"Evidence, sign-off, cutover" (cutover gate contract)
  </read_first>
  <action>
    Review the evidence bundle from top to bottom and perform three consolidation passes. Do NOT re-do Plans 02/03/04's work — only fill gaps and update summary lines.

    **Pass 1 — Frontmatter update:**
    Ensure frontmatter has the final shape:
    ```yaml
    ---
    phase: 04-validation
    verified: {YYYY-MM-DD of final sign-off run, or "pending" if still awaiting firm}
    status: {verified | human_needed | pending}
    score: {N}/3 SCs verified
    external_blocker: {...existing...}
    sign_off:
      firm_rep: {name, or "pending"}
      date: {YYYY-MM-DD, or "pending"}
      medium: {email | slack | other, or "pending"}
      quote: |
        {verbatim or "pending"}
    ---
    ```
    - `score` must match the count of SCs that show ✅ in the body.
    - `status: verified` ONLY if all three SCs show ✅ AND sign_off is fully populated. Otherwise `human_needed` (for missing sign-off) or `pending` (for missing SCs).

    **Pass 2 — Cutover gate checklist:**
    Find the `## Cutover gate` section near the end of the bundle. Ensure all four checkboxes reflect reality:
    ```markdown
    ## Cutover gate

    - [ {X or space}] TEST-01 ✅
    - [ {X or space}] TEST-02 ✅
    - [ {X or space}] TEST-03 ✅ (all four scenarios)
    - [ {X or space}] Firm sign-off received
    ```

    Every ticked box MUST have corresponding `Verdict: ✅` or equivalent marker above in the relevant SC section. If any SC section shows `❌ TRUNCATED`, `❌ DRIFTED`, `⬜ pending`, or missing evidence → that checkbox is NOT ticked.

    **Pass 3 — Sign-off footer:**
    Find the `### Firm sign-off` sub-section. Populate or update:

    IF firm has replied:
    ```markdown
    **Rep name:** {e.g. "Partner X Lastname"}
    **Date:** {YYYY-MM-DD}
    **Medium:** {email | Slack | other}
    **Verbatim quote:**
    > {Paste the exact reply from the firm — preserve quoting/newlines. MUST contain "approved" or equivalent explicit ack. Example: "Reviewed 04-UAT-EVIDENCE.md — all three tests green. Approved to cutover. — Partner X"}
    ```

    IF firm has NOT yet replied (at the time this plan runs):
    ```markdown
    **Rep name:** pending
    **Date:** pending
    **Medium:** pending
    **Verbatim quote:**
    > pending — evidence bundle sent to firm on {YYYY-MM-DD} via {email | Slack | other}. Awaiting reply. DO NOT CUTOVER until this footer is filled with an explicit approval.
    ```

    The "pending" state is acceptable if the operator has sent the bundle but the firm hasn't replied yet — but cutover MUST block until this is filled with a real quote.

    **Pass 4 — Final consistency check:**
    - `grep -E "\[[xX]\] TEST-01" 04-UAT-EVIDENCE.md` — if ticked, TEST-01 section must show a positive result (log snippets + screenshots present, no "pending" placeholders remaining in TEST-01 body)
    - `grep -E "\[[xX]\] TEST-02" 04-UAT-EVIDENCE.md` — if ticked, TEST-02 Verdict line MUST say `MATCH` (not TRUNCATED or DRIFTED)
    - `grep -E "\[[xX]\] TEST-03" 04-UAT-EVIDENCE.md` — if ticked, ALL FOUR §4.x sub-rows (§4.1-§4.4) must be populated
    - If ANY of these inconsistencies are found: unticket the top-level checkbox and update `status:` frontmatter to `human_needed` with a note at the top of the bundle explaining what is blocking.
  </action>
  <verify>
    <automated>grep -cE "^- \[[xX ]\] TEST-0[123]" .planning/phases/04-validation/04-UAT-EVIDENCE.md | awk '{exit ($1 < 3)}' && grep -cE "^- \[[xX ]\] Firm sign-off" .planning/phases/04-validation/04-UAT-EVIDENCE.md | awk '{exit ($1 < 1)}' && grep -cE "^score: [0-9]/3 SCs" .planning/phases/04-validation/04-UAT-EVIDENCE.md | awk '{exit ($1 < 1)}' && grep -cE "^  firm_rep:" .planning/phases/04-validation/04-UAT-EVIDENCE.md | awk '{exit ($1 < 1)}' && grep -cE "^  medium:" .planning/phases/04-validation/04-UAT-EVIDENCE.md | awk '{exit ($1 < 1)}' && grep -cE "^  quote:" .planning/phases/04-validation/04-UAT-EVIDENCE.md | awk '{exit ($1 < 1)}' && grep -cE "^\*\*Rep name:\*\*" .planning/phases/04-validation/04-UAT-EVIDENCE.md | awk '{exit ($1 < 1)}' && grep -cE "^\*\*Verbatim quote:\*\*" .planning/phases/04-validation/04-UAT-EVIDENCE.md | awk '{exit ($1 < 1)}'</automated>
  </verify>
  <acceptance_criteria>
    - Frontmatter `score:` line reflects actual count: `grep -cE "^score: [0-3]/3 SCs verified$" 04-UAT-EVIDENCE.md` returns 1
    - Frontmatter `sign_off:` block has all four keys (firm_rep, date, medium, quote) — `grep -cE "^  (firm_rep|date|medium|quote):" 04-UAT-EVIDENCE.md` returns at least 4
    - Cutover-gate checklist has all four items: `grep -cE "^- \[[xX ]\] (TEST-0[123]|Firm sign-off)" 04-UAT-EVIDENCE.md` returns at least 4
    - Firm sign-off footer populated (not `pending` — real name + date + quote) if `grep -E "^- \[[xX]\] Firm sign-off received" 04-UAT-EVIDENCE.md` matches; otherwise quote can be `pending` with explanation
    - No `{paste` or `{fill in}` placeholder strings remain in TEST-01/02/03 sections (`grep -cE "\\{paste|\\{fill in\\}" 04-UAT-EVIDENCE.md` returns 0)
    - If `status: verified` in frontmatter: all three TEST-XX checkboxes are ticked AND Firm sign-off checkbox is ticked AND firm quote is NOT `pending`
    - If `status: human_needed` or `pending` in frontmatter: at least one of the above is NOT true, and the bundle has a top note explaining what blocks cutover
  </acceptance_criteria>
  <done>04-UAT-EVIDENCE.md has consistent frontmatter, fully-populated cutover-gate checklist matching SC body states, sign-off footer real-or-pending with explicit block-note, zero placeholder strings in populated sections.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Send evidence bundle to firm + capture verbatim sign-off reply</name>
  <files>.planning/phases/04-validation/04-UAT-EVIDENCE.md</files>
  <action>Operator emails (or Slacks) the firm rep with subject "Phase 4 UAT Evidence Bundle ready for review" and body referencing the committed 04-UAT-EVIDENCE.md (PR URL or branch link), summarising the three SC outcomes inline, and requesting a reply ("approved" or concerns). Wait for the firm's reply — do NOT cutover without one. When reply arrives, copy the firm's exact text into the "Verbatim quote" block under "### Firm sign-off" in 04-UAT-EVIDENCE.md, populate frontmatter sign_off (firm_rep, date, medium, quote), tick the `- [x] Firm sign-off received` checkbox, and set frontmatter `status: verified` + `score: 3/3 SCs verified` + `verified: <YYYY-MM-DD>`. If firm rejects or flags issues: do NOT tick the sign-off box, set `status: human_needed`, capture concerns verbatim, and create a gap-closure plan (04-06 or similar) to address.</action>
  <what-built>
    Fully-consolidated `04-UAT-EVIDENCE.md` + fleshed-out `04-RUNBOOK.md` §9 rollback section. This checkpoint is the final cutover-gate verification: the firm reviews the bundle and replies with explicit approval or rejection.
  </what-built>
  <how-to-verify>
    1. **Send the bundle to the firm:**
       - Email (or Slack) the firm rep with subject/message:
         > "Phase 4 UAT Evidence Bundle ready for review. All three success criteria marked {✅/pending}. Please review `.planning/phases/04-validation/04-UAT-EVIDENCE.md` (attached or committed to <branch>). Reply 'approved' or describe any concerns. This is the cutover gate — production deploy is blocked until you reply."
       - Attach or reference: (a) the committed `04-UAT-EVIDENCE.md` URL (GitHub PR or branch link), (b) a summary of the three SC outcomes, (c) the screenshots inline or as attachments.

    2. **Wait for firm reply.** Do NOT proceed to cutover without a reply.

    3. **Capture reply verbatim:**
       - Copy the firm's exact reply (email body or Slack message).
       - Edit `04-UAT-EVIDENCE.md` "Firm sign-off" section. Replace the `pending` quote with the actual reply, preserving all newlines, punctuation, and signatures.
       - Update frontmatter `sign_off:` block accordingly (firm_rep name, date, medium).
       - Tick the `- [x] Firm sign-off received` checkbox.
       - Update frontmatter `status: verified` + `score: 3/3 SCs verified` + `verified: {YYYY-MM-DD}`.

    4. **Final sanity check:**
       - `grep -E "^status: verified$" 04-UAT-EVIDENCE.md` matches
       - `grep -E "^score: 3/3 SCs verified$" 04-UAT-EVIDENCE.md` matches
       - `grep -E "^- \[x\] (TEST-0[123]|Firm sign-off)" 04-UAT-EVIDENCE.md` returns 4 matches
       - Quote block under "Verbatim quote:" contains the firm's actual text (no "pending", no placeholder)
       - `grep -iE "approved|go ahead|cutover|proceed|ship" 04-UAT-EVIDENCE.md` matches at least once in the quote area (explicit ack)

    5. **If firm rejects or flags issues:**
       - Do NOT tick the sign-off checkbox.
       - Update `status: human_needed` in frontmatter.
       - Capture the firm's concerns verbatim.
       - Create a new plan (04-06 or a gap-closure plan) to address the concerns, OR escalate for a scope discussion.
  </how-to-verify>
  <verify>
    <automated>grep -E "^status: verified$" .planning/phases/04-validation/04-UAT-EVIDENCE.md && grep -E "^score: 3/3 SCs verified$" .planning/phases/04-validation/04-UAT-EVIDENCE.md && grep -cE "^- \[x\] (TEST-0[123]|Firm sign-off)" .planning/phases/04-validation/04-UAT-EVIDENCE.md | awk '{exit ($1 < 4)}' && ! grep -E "pending" .planning/phases/04-validation/04-UAT-EVIDENCE.md | grep -i "quote"</automated>
  </verify>
  <done>Firm's verbatim sign-off reply captured in 04-UAT-EVIDENCE.md footer + frontmatter; all four cutover-gate checkboxes ticked; frontmatter status=verified + score=3/3; production cutover is unblocked. (If rejected: status=human_needed with concerns quoted and a gap-closure plan referenced.)</done>
  <resume-signal>Type "firm approved — bundle signed off" (with the verbatim quote filled in) OR "firm flagged {issue}" (describe what needs to change) OR "still awaiting firm reply since {date}" (escalation)</resume-signal>
</task>

</tasks>

<verification>
- `grep -E "^status: verified$" .planning/phases/04-validation/04-UAT-EVIDENCE.md` matches (after firm sign-off)
- `grep -E "^score: 3/3 SCs verified$" .planning/phases/04-validation/04-UAT-EVIDENCE.md` matches
- `grep -cE "^- \[x\]" .planning/phases/04-validation/04-UAT-EVIDENCE.md` returns 4 (all cutover-gate boxes ticked)
- `grep -c "^### §9\." .planning/phases/04-validation/04-RUNBOOK.md` returns at least 4 (rollback subsections)
- No placeholder strings (`{paste`, `{fill in}`, `pending` in quote) remain in populated sections
- Firm quote contains an explicit approval keyword (`approved`, `proceed`, `cutover`, `ship`, or equivalent)
</verification>

<success_criteria>
- `04-UAT-EVIDENCE.md` is the definitive "OK to cutover" artifact: 3/3 SCs ✅ + firm sign-off ✅
- `04-RUNBOOK.md` §9 has actionable rollback commands (Vercel CLI + dashboard + manual fan-out replay + escalation)
- Phase 4 exit gate: MET — production cutover is unblocked
- TEST-01, TEST-02, TEST-03 requirements all satisfied
- Firm has explicitly acknowledged in writing — quote captured verbatim
</success_criteria>

<output>
After completion, create `.planning/phases/04-validation/04-05-SUMMARY.md` capturing: final cutover-gate state (3/3 or partial), firm sign-off quote + date + medium, rollback runbook expansion (commands added), any deferred items (e.g. opportunistic raw-body logging if not folded in), and the next action (production cutover or gap-closure if any SC is not ✅).
</output>
