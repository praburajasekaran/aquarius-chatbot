---
phase: 03-webhook-cleanup
plan: 04
type: execute
wave: 4
depends_on: ["03-02", "03-03"]
files_modified:
  - src/lib/stripe.ts
  - src/app/api/webhooks/stripe/route.ts
  - src/scripts/revoke-upload-token.ts
  - .env.example
  - .planning/codebase/INTEGRATIONS.md
  - package.json
  - package-lock.json
autonomous: true
requirements: [CLEAN-01, CLEAN-02, CLEAN-03]

must_haves:
  truths:
    - "src/lib/stripe.ts and src/app/api/webhooks/stripe/route.ts no longer exist on disk"
    - "src/scripts/revoke-upload-token.ts uses bpoint-txn:${sessionId} (not stripe-session:)"
    - ".env.example contains no STRIPE_* entries and no DEPRECATED Stripe comment block"
    - ".planning/codebase/INTEGRATIONS.md contains no Stripe references (Payment Processing section deprecated subsection removed, STRIPE_* removed from Required env vars, Incoming Webhooks #1 removed)"
    - "package.json dependencies do not contain stripe, @stripe/stripe-js, or @stripe/react-stripe-js"
    - "npm run build, npm run lint, and npm test all exit 0 after full cleanup"
  artifacts:
    - path: "src/lib/stripe.ts"
      provides: "DELETED — no Stripe singleton or createCheckoutSession in codebase"
      contains: "MUST NOT EXIST"
    - path: "src/app/api/webhooks/stripe/route.ts"
      provides: "DELETED — Stripe webhook handler removed"
      contains: "MUST NOT EXIST"
    - path: "src/scripts/revoke-upload-token.ts"
      provides: "dedup key prefix renamed stripe-session: → bpoint-txn:"
      contains: "bpoint-txn:"
    - path: ".env.example"
      provides: "STRIPE_* entries and DEPRECATED comment block removed"
      contains: "NO_STRIPE_ENTRIES"
    - path: ".planning/codebase/INTEGRATIONS.md"
      provides: "all Stripe references scrubbed (3 sections: Payment Processing, Required env vars, Incoming Webhooks)"
      contains: "NO_STRIPE_REFS"
    - path: "package.json"
      provides: "stripe, @stripe/stripe-js, @stripe/react-stripe-js removed from dependencies"
      contains: "NO_STRIPE_DEPS"
  key_links:
    - from: "src/scripts/revoke-upload-token.ts"
      to: "Redis key namespace"
      via: "dedupeKey = `bpoint-txn:${sessionId}`"
      pattern: "bpoint-txn:\\$\\{sessionId\\}"
---

<objective>
Sequenced Stripe removal: delete Stripe source files, rename the last Stripe Redis prefix in the revoke-upload-token script, scrub Stripe env vars from docs/.env.example, and uninstall the three Stripe npm packages. End state: zero Stripe surface area in code, config, docs, or dependencies.

Purpose: Close out the Stripe→BPoint migration by removing the deprecated provider entirely. Soak period is over (Phase 02 VERIFICATION confirmed the BPoint confirm route works; Plan 02 added the webhook safety net; Plan 03 ported the last non-deletion caller). This is the irreversible cutover step.
Output:
  - Two Stripe source files deleted
  - revoke-upload-token.ts uses bpoint-txn: prefix
  - .env.example and INTEGRATIONS.md fully scrubbed
  - package.json / package-lock.json reflect the uninstall
  - Green build, lint, and test suite
</objective>

<execution_context>
@/Users/praburajasekaran/.claude/get-shit-done/workflows/execute-plan.md
@/Users/praburajasekaran/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-webhook-cleanup/03-CONTEXT.md
@.planning/phases/03-webhook-cleanup/03-RESEARCH.md
@.planning/phases/03-webhook-cleanup/03-03-SUMMARY.md
@src/lib/stripe.ts
@src/app/api/webhooks/stripe/route.ts
@src/scripts/revoke-upload-token.ts
@.env.example
@.planning/codebase/INTEGRATIONS.md
@package.json

<interfaces>
<!-- Reference state extracted before deletion. -->

Files to delete (confirmed to exist pre-plan):
- src/lib/stripe.ts (50 lines — singleton + createCheckoutSession + PRICING re-export)
- src/app/api/webhooks/stripe/route.ts (130 lines — Stripe POST handler)

Redis prefix rename target (src/scripts/revoke-upload-token.ts line 18):
```typescript
const dedupeKey = `stripe-session:${sessionId}`;  // BEFORE
const dedupeKey = `bpoint-txn:${sessionId}`;      // AFTER
```
Also update the JSDoc comment on line 2 (`for a given Stripe session` → `for a given BPoint transaction`).

.env.example lines to delete (lines 13-21 as confirmed pre-plan read):
```
# -----------------------------------------------------------------------------
# Stripe — DEPRECATED, replaced by BPoint for Trust Accounting settlement.
# Retained during BPoint rollout soak period (1 week of clean prod usage),
# then the Stripe wiring is removed per design spec.
# -----------------------------------------------------------------------------
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
```

package.json dependencies to remove (lines 18-19, 32 as confirmed pre-plan read):
- `"@stripe/react-stripe-js": "^6.1.0"`
- `"@stripe/stripe-js": "^9.1.0"`
- `"stripe": "^22.0.1"`

.planning/codebase/INTEGRATIONS.md Stripe references (from pre-plan grep):
- Lines 16-22: Payment Processing → "Stripe (DEPRECATED, migration in progress)" subsection — DELETE entirely
- Line 102: "Stripe deduplication: `stripe-session:${sessionId}`..." — REPLACE with BPoint dedup: `bpoint-txn:${TxnNumber}` with 7d TTL
- Line 141: "[stripe-webhook]" example in logging prefix list — REPLACE with "[bpoint-webhook]" or "[bpoint-confirm]"
- Lines 169-170: STRIPE_SECRET_KEY / STRIPE_WEBHOOK_SECRET in Required env vars — DELETE both lines
- Lines 206-209: "Stripe checkout completion - POST /api/webhooks/stripe" — DELETE entirely (renumber remaining webhooks 2 and 3 → 1 and 2)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rename Redis prefix + delete Stripe source files</name>
  <files>src/scripts/revoke-upload-token.ts, src/lib/stripe.ts, src/app/api/webhooks/stripe/route.ts</files>

  <read_first>
    - src/scripts/revoke-upload-token.ts (current — line 18 has `stripe-session:` prefix; line 2 JSDoc references "Stripe session")
    - src/lib/stripe.ts (targeted for deletion)
    - src/app/api/webhooks/stripe/route.ts (targeted for deletion)
    - .planning/phases/03-webhook-cleanup/03-03-SUMMARY.md (confirm no other caller of @/lib/stripe exists)
    - .planning/phases/03-webhook-cleanup/03-RESEARCH.md §"Pitfall 1" (deletion order rationale — resume port already done in Plan 03)
  </read_first>

  <action>
STEP A — Rename Redis prefix in src/scripts/revoke-upload-token.ts:

1) Update the JSDoc comment on line 2 from:
   `* Revoke an active late-upload token for a given Stripe session.`
   to:
   `* Revoke an active late-upload token for a given BPoint transaction.`

2) Update the usage comment on line 4 from:
   `*   npx tsx src/scripts/revoke-upload-token.ts --session <bpointTxnNumber>`
   (already correct — but verify it reads bpointTxnNumber, not stripeSessionId. If it still says stripeSessionId, fix to bpointTxnNumber.)

3) Change line 18 from:
   `  const dedupeKey = \`stripe-session:${sessionId}\`;`
   to:
   `  const dedupeKey = \`bpoint-txn:${sessionId}\`;`

4) No other changes in that file. Do NOT change the CLI arg name `--session` or any other business logic.

STEP B — Delete Stripe source files:

1) Delete `src/lib/stripe.ts` (whole file):
   `rm src/lib/stripe.ts`

2) Delete `src/app/api/webhooks/stripe/route.ts` and the now-empty directory:
   `rm src/app/api/webhooks/stripe/route.ts`
   `rmdir src/app/api/webhooks/stripe 2>/dev/null || true`  (only if the directory is now empty; if it contains other files — which it shouldn't — leave it alone)

3) Verify no orphaned imports remain:
   `grep -rln "@/lib/stripe" src/` must output nothing.
   If it lists any file, STOP — Plan 03's verification check must have missed a caller. Do not proceed to Task 2 until this is empty.

STEP C — Build and lint after deletion:

Run `npm run build && npm run lint && npm test` to confirm TypeScript, ESLint, and vitest all report zero issues after the deletions. All three must exit 0 before proceeding to Task 2.
  </action>

  <verify>
    <automated>npm run build && npm run lint && npm test</automated>
  </verify>

  <acceptance_criteria>
    - `test ! -f src/lib/stripe.ts` (file does not exist)
    - `test ! -f src/app/api/webhooks/stripe/route.ts` (file does not exist)
    - `grep -rln "@/lib/stripe" src/` outputs nothing (zero lines)
    - `grep -c "stripe-session:" src/scripts/revoke-upload-token.ts` returns 0
    - `grep -c "bpoint-txn:" src/scripts/revoke-upload-token.ts` returns at least 1
    - `grep -q "BPoint transaction" src/scripts/revoke-upload-token.ts` (JSDoc updated)
    - `grep -rln "stripe-session:" src/` outputs nothing (last caller renamed)
    - `npm run build` exits 0
    - `npm run lint` exits 0
    - `npm test` exits 0 (49 tests pass — no regression from deletion)
  </acceptance_criteria>

  <done>
    Two Stripe source files deleted. revoke-upload-token.ts uses bpoint-txn: prefix. No @/lib/stripe imports remain in src/. Build, lint, and tests all green.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Scrub STRIPE_* from .env.example and INTEGRATIONS.md</name>
  <files>.env.example, .planning/codebase/INTEGRATIONS.md</files>

  <read_first>
    - .env.example (lines 13-21 — the Stripe comment block + three env vars to delete)
    - .planning/codebase/INTEGRATIONS.md (lines 16-22, 102, 141, 169-170, 206-209 — six Stripe touchpoints per pre-plan grep)
  </read_first>

  <action>
STEP A — .env.example:

Delete lines 13-21 inclusive. That is, remove the entire block:

```
# -----------------------------------------------------------------------------
# Stripe — DEPRECATED, replaced by BPoint for Trust Accounting settlement.
# Retained during BPoint rollout soak period (1 week of clean prod usage),
# then the Stripe wiring is removed per design spec.
# -----------------------------------------------------------------------------
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
```

Do not collapse the surrounding blank lines. The OpenRouter block above (lines 7-11) and the BPoint block below (starting line 23 pre-edit) should have one blank line between them after the deletion — same spacing pattern used elsewhere in the file.

Do NOT touch .env.local (03-CONTEXT.md §"Env var scrubbing" explicitly excludes it — gitignored developer-local file).

STEP B — .planning/codebase/INTEGRATIONS.md:

Apply these six edits, using exact anchor text for each:

1) DELETE the "Stripe (DEPRECATED, migration in progress)" subsection — the 7-line block from "- Stripe (DEPRECATED, migration in progress)" through "- Note: Being replaced by BPoint; Stripe config retained during soak period". Keep the blank line after, then the BPoint subsection immediately follows.

2) REPLACE the Stripe dedup line (approx line 102):
   FROM: `    - Stripe deduplication: \`stripe-session:${sessionId}\` with 7d TTL (prevents double-processing)`
   TO:   `    - BPoint payment dedup: \`bpoint-txn:${TxnNumber}\` with 7d TTL (shared by confirm route + webhook; prevents double fan-out)`

3) REPLACE the log prefix example (approx line 141):
   FROM: `  - Prefixed with service name (e.g., "[stripe-webhook]", "[upload]", "[smokeball-capture]")`
   TO:   `  - Prefixed with service name (e.g., "[bpoint-webhook]", "[bpoint-confirm]", "[upload]", "[smokeball-capture]")`

4) DELETE these two lines in the Required env vars list (approx lines 169-170):
   `- \`STRIPE_SECRET_KEY\` - Payment processing (deprecated but required during migration)`
   `- \`STRIPE_WEBHOOK_SECRET\` - Webhook verification`

5) ADD to the Required env vars list (insert AFTER `OPENROUTER_API_KEY` line):
   `- \`BPOINT_API_USERNAME\`, \`BPOINT_API_PASSWORD\`, \`BPOINT_MERCHANT_NUMBER\` - BPoint payment processing`
   `- \`BPOINT_ENV\` - BPoint environment flag ("prod" for live; anything else sets IsTestTxn=true)`
   AND DELETE the corresponding entries from the "Optional env vars" list (the BPOINT_* lines near line 189-190 — BPoint is no longer optional).

6) REPLACE the Incoming Webhook #1 entry (approx lines 206-209):
   FROM (4 lines):
   ```
   1. Stripe checkout completion - `POST /api/webhooks/stripe`
      - Verified via: `stripe.webhooks.constructEvent(body, signature, secret)`
      - Event type: `checkout.session.completed`
      - Payload: Session ID, customer email, payment amount
   ```
   TO (4 lines):
   ```
   1. BPoint server-to-server payment callback - `POST /api/webhooks/bpoint`
      - Verified via: server-side `retrieveTransaction(ResultKey)` call (callback body NOT trusted; BPoint v2 callbacks are unsigned)
      - Trigger: Transaction completion; safety net for missed browser redirect
      - Payload: `ResultKey` from URL query string — body intentionally ignored
   ```

The remaining webhooks (Calendly, Smokeball capture-back) stay as items 2 and 3 with original numbering. Do NOT renumber them.

After edits, verify with grep (see acceptance_criteria). If any "Stripe" or "STRIPE_" still appears in INTEGRATIONS.md, re-read the file and find/fix the leftover reference.
  </action>

  <verify>
    <automated>test $(grep -c "STRIPE_" .env.example) -eq 0 && test $(grep -ciE "stripe" .planning/codebase/INTEGRATIONS.md) -eq 0 && test $(grep -c "BPOINT_API_USERNAME" .planning/codebase/INTEGRATIONS.md) -ge 1</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "STRIPE_" .env.example` returns 0
    - `grep -c "stripe" .env.example` (case-insensitive: `grep -ci`) returns 0
    - `grep -ci "stripe" .planning/codebase/INTEGRATIONS.md` returns 0 (case-insensitive; Stripe references fully removed)
    - `grep -c "BPOINT_API_USERNAME" .planning/codebase/INTEGRATIONS.md` returns at least 1 (BPoint promoted to Required env vars)
    - `grep -c "bpoint-txn:" .planning/codebase/INTEGRATIONS.md` returns at least 1 (dedup namespace documented)
    - `grep -c "POST /api/webhooks/bpoint" .planning/codebase/INTEGRATIONS.md` returns at least 1 (BPoint webhook documented)
    - `grep -c "POST /api/webhooks/stripe" .planning/codebase/INTEGRATIONS.md` returns 0
    - Line counts roughly align: INTEGRATIONS.md line count should drop by ~10 (Stripe subsection deleted) plus minor churn; .env.example should drop by 9 lines (8 STRIPE block lines + possibly one blank). Neither file should grow.
  </acceptance_criteria>

  <done>
    Zero Stripe references remain in .env.example or .planning/codebase/INTEGRATIONS.md. BPoint is documented as a Required env var set and as the primary incoming-webhook entry.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Uninstall Stripe packages + verify clean build</name>
  <files>package.json, package-lock.json</files>

  <read_first>
    - package.json (confirm `stripe`, `@stripe/stripe-js`, `@stripe/react-stripe-js` are still in dependencies pre-task; they should be because nothing has removed them yet)
    - .planning/phases/03-webhook-cleanup/03-RESEARCH.md §"Pitfall 4" (watch for orphaned Stripe type references post-uninstall)
  </read_first>

  <action>
STEP A — Uninstall the three Stripe packages in a single command:

```bash
npm uninstall stripe @stripe/stripe-js @stripe/react-stripe-js
```

This updates both package.json and package-lock.json atomically. Do NOT manually edit package.json to remove the entries — use npm so lockfile hashes stay consistent.

STEP B — Verify nothing in the codebase still references Stripe:

Run in sequence:
```bash
npm run build   # must exit 0
npm run lint    # must exit 0
npm test        # must exit 0 — 49 tests
```

If `npm run build` fails with a "Cannot find module 'stripe'" or "Cannot find module '@stripe/...'" error, STOP. Some file still imports from a Stripe package. Find it via:
```bash
grep -rln "from [\"']stripe[\"']\\|from [\"']@stripe/" src/
```
Fix the leftover import (most likely case: a type-only import in a file that wasn't cleaned up earlier). Then re-run the three verification commands.

STEP C — Final consistency check (no grep on node_modules):

```bash
# There must be no mention of Stripe as a direct dependency
grep -E '"stripe"\\s*:|"@stripe/stripe-js"\\s*:|"@stripe/react-stripe-js"\\s*:' package.json
# Expected: no output
```

If the grep above returns any line, `npm uninstall` did not fully rewrite package.json — investigate and re-run the uninstall.

Do NOT delete or edit package-lock.json manually. npm manages it.
Do NOT use `npm prune` or `npm audit fix` as part of this task (out of scope — only the three Stripe packages are targeted).
  </action>

  <verify>
    <automated>npm run build && npm run lint && npm test && test $(grep -cE '"stripe"\s*:|"@stripe/stripe-js"\s*:|"@stripe/react-stripe-js"\s*:' package.json) -eq 0</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c '"stripe":' package.json` returns 0
    - `grep -c '"@stripe/stripe-js":' package.json` returns 0
    - `grep -c '"@stripe/react-stripe-js":' package.json` returns 0
    - `grep -c "stripe" package.json` returns 0 (case-sensitive)
    - `grep -rln 'from "stripe"' src/` returns no matches
    - `grep -rln 'from "@stripe/' src/` returns no matches
    - `npm run build` exits 0
    - `npm run lint` exits 0
    - `npm test` exits 0 (49 tests pass)
    - `test -f package-lock.json` (lockfile still exists — don't accidentally delete it)
    - The pre-plan test count is preserved: `npm test 2>&1 | grep -E "Tests\\s+[0-9]+ passed"` shows at least 49 passing tests
  </acceptance_criteria>

  <done>
    Stripe is fully removed from the codebase: three npm packages uninstalled, two source files deleted, env vars scrubbed from .env.example and INTEGRATIONS.md, Redis prefix renamed, build/lint/test all green. Zero Stripe surface area.
  </done>
</task>

</tasks>

<verification>
- `test ! -f src/lib/stripe.ts && test ! -f src/app/api/webhooks/stripe/route.ts` (deleted files stay deleted)
- `grep -rln "@/lib/stripe\\|from \"stripe\"\\|from \"@stripe/" src/` returns no matches (zero Stripe imports anywhere in src/)
- `grep -c "STRIPE_" .env.example` returns 0
- `grep -ci "stripe" .planning/codebase/INTEGRATIONS.md` returns 0
- `grep -c "stripe-session:" src/` via `grep -rc "stripe-session:" src/` returns 0 (Redis prefix migration complete)
- `grep -c '"stripe":\\|"@stripe/' package.json` returns 0 (packages uninstalled)
- `npm run build && npm run lint && npm test` — all three exit 0
- 49 tests passing (40 pre-phase-3 + 9 new webhook tests)
</verification>

<success_criteria>
- CLEAN-01: `stripe`, `@stripe/stripe-js`, `@stripe/react-stripe-js` absent from package.json dependencies
- CLEAN-02: `src/lib/stripe.ts` and `src/app/api/webhooks/stripe/route.ts` deleted; no code references Stripe
- CLEAN-03: STRIPE_* vars removed from .env.example and from .planning/codebase/INTEGRATIONS.md
- Supplementary: Redis prefix in revoke-upload-token.ts is bpoint-txn: (aligns with Phase 2 dedup namespace)
- Supplementary: `npm run build && npm run lint && npm test` all green — migration is the new ground state
</success_criteria>

<output>
After completion, create `.planning/phases/03-webhook-cleanup/03-04-SUMMARY.md` capturing:
- Files deleted (with original line counts)
- File edits (.env.example line delta; INTEGRATIONS.md line delta)
- package.json before/after dependency counts
- `npm uninstall` output verbatim (for the uninstall log)
- Post-cleanup test count (must be 49 unchanged)
- Explicit note: "Production Vercel env scrub is an ops task — Aquarius must remove STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET, NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY from Vercel dashboard before production cutover" (03-CONTEXT.md §"Env var scrubbing" — not a code change, but flag it here so the Phase 3 handoff captures it).
</output>
